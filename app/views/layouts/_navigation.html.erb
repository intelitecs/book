<div class="row">
    <div class="top-bar">
      <ul class="title-area">
        <!-- Title Area  -->
        <li class="name"><h1><%= link_to "Le Book de Leti", root_path %></h1></li>
      </ul>

      <section class="top-bar-section">
        <!-- Left Nav section -->
        <ul class="left">
          <li class="divider"></li>
          <% unless !current_page?('/users') %>
            <li><%= link_to "Oeuvres",root_path %></li>
          <% end %>
          <li class="divider"></li>
          <li><%= link_to "Profile de Lety","#" %></li>
          <% unless !admin_user? %>
              <li class="divider"></li>
              <li><%= link_to "Utilisateurs",users_path %></li>
              <li class="divider"></li>
          <% end %>



        </ul>

        <!-- Right Nav section  -->

          <ul class="right" nav-menu>
                    <li class="divider"></li>

               <% if signed_in?  %>
                    <li><%= link_to "[#{current_user.username}]",  user_path(current_user) %></li>
                    <li class="divider"></li>
                    <li><%= link_to "[Déconnexion]" , logout_path %></li>

                <% else %>
                    <% unless !current_page?("/")  %>
                        <li class="divider"></li>
                        <li><a href="/login"   class="connexion">Log In</a></li>
                        <li class="divider"></li>
                        <li><a href="/sign_up"   class="connexion">Sign up</a></li>
                    <% else if current_page?("/login") %>
                            <li><a href="/sign_up"  class="connexion">Sign up</a></li>
                        <% else  %>
                            <li><a href="/login"  class="connexion">Log In</a></li>
                            <li class="divider"></li>
                            <li><a href="/sign_up"  class="connexion">Sign up</a></li>
                        <% end %>
                    <% end %>
               <% end %>


          </ul>
      </section>

    </div>


</div>